<h1>All collabs</h1>

<div class="flickr_pagination">
  <%= will_paginate @collabs, :container => false %>
</div>

<%= render 'collab' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @collabs, :container => false %>
</div>

