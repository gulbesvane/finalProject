<h1>Project showcase</h1>

<% @tags.each do |tag| %>
<h1><%= link_to tag, "/tags/#{tag}" %></h1>
<% end %> 

<div class="flickr_pagination">
  <%= will_paginate @posts, :container => false %>
</div>

<%= render 'post' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @posts, :container => false %>
</div>
